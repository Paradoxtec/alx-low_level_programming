/**
 * main - Entry
 * Deacription: determine if a number is positive, negative or zero
 *
 * Return: Always 0 (success)
 */

#include<stdlib.h>
#include<time.h>
#include<stdio.h>

int main(void)
{       
        int n;
        strand(time(0));
        n = rand() - RAND_MAX / 2;
        if (n > 0)
        {       
                printf("%d is positive\n" , n);
        }       
        else if ( n == 0)
{
printf("%d is zero\n" , n);
}
else
{
printf(
